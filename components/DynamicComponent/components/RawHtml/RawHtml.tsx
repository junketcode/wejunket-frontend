import * as React from "react"

// TODO: Make this component in order to create WYSWYG Editor in the back-end
// WIP.
const RawHTML = (props: any) => {
  const { htmlToRender } = props

  return (
    <div>Lol din't work {htmlToRender}</div>
  )
}

export default RawHTML
